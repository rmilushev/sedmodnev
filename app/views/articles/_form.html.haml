= simple_form_for(@article) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :content
    -# = f.input :published
    = f.input :picture, as: :file
    -# = f.input :author_id
    -# = f.input :category_id
    -# = f.input :golive

  .form-actions
    = f.button :submit
